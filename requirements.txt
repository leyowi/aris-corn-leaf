# ARIS - dev machine requirements
#
# NOTE: torch/torchvision below install CPU-only via plain `pip install -r requirements.txt`.
# For CUDA-accelerated training (NVIDIA GPU), install torch/torchvision separately FIRST with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# then run `pip install -r requirements.txt` — pip will see torch/torchvision already
# satisfied and skip reinstalling the CPU build.

# YOLOv8 detection + segmentation
ultralytics
torch
torchvision

# dataset pulling from Roboflow
roboflow

# data handling & analysis
pandas
numpy
matplotlib
seaborn

# model export
onnx
onnxruntime

# notebooks
jupyter
ipykernel

# config & utilities
pyyaml
python-dotenv
tqdm

# code quality
black
flake8